{
  "name": "Particular CI workflow with dependency support",
  "description": "A CI workflow template to build and test packages that have infrustructure 
  dependencies. This template is necessary if the build/test pipeline requires use of secured 
  resources such as Azure or AWS credentials.",
  "iconName": "particular"
}
